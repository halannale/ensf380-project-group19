package edu.ucalgary.oop;

public class SchedulePrint {
    public void printDetails(String[] args) {
        if (backupVolunteer.exists()){
            String time = assignTime.time() + backupVolunteer.exists();
            for (int i = 0; i> assignTime.medicalTreatment(); i++){
                String medTreatment= assignTime.medicalTreatment();
            }
            for (int i = 0; i> assignTime.feeding(); i++){
                String feeding= assignTime.medicalTreatment();
            }
        }
        else{
            String time = assignTime.time();
            for (int i = 0; i> assignTime.medicalTreatment(); i++){
                String medTreatment= assignTime.medicalTreatment();
            }
            for (int i = 0; i> assignTime.feeding(); i++){
                String feeding= assignTime.medicalTreatment();
            }
        }

    }
}
